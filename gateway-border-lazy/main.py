# This is a sample Python script.

# Press Shift+F10 to execute it or replace it with your code.
# Press Double Shift to search everywhere for classes, files, tool windows, actions, and settings.

from console import start
# pegar o token no devconsole
# salvar em arquivo o token
# solicitar a api que quer chamar
    # extrair o host - e gerar o token + host no gateway border
    # salvar o token, com o horário para validação de 30 mins
# mostrar o response
# perguntar se quer fazer outra, com outros dados? MVP 2
# caso dê erro de token expirado, solicitar novo token, atualizar e fazer a chamada

def print_hi(name):
   start()


# Press the green button in the gutter to run the script.
if __name__ == '__main__':
    print_hi('PyCharm')

# See PyCharm help at https://www.jetbrains.com/help/pycharm/
